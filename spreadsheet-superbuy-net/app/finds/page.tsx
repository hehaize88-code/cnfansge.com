import { SitePage } from "../site-client";
import { pageMetadata } from "../seo";
export const metadata=pageMetadata("/finds/","Superbuy Finds: Current Product Routes","Review current Superbuy product finds by category with route dates, USD references and direct listing checks.");
export default function Page(){ return <SitePage page="finds"/>; }
